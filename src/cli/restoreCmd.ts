import { Command } from "commander";

export const restoreCmd = new Command("restore")
  .description("Restore command placeholder")
  .action(() => {
    console.log("Restore command not implemented yet.");
  });